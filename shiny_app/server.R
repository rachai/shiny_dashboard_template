# Define server logic 
server <- function(input, output) {
  tab1_item1$server(input, output)
  tab1_item2$server(input, output)
  tab1_item3$server(input, output)
  tab1_item4$server(input, output)
}
